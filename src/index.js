import readlineSync from 'readline-sync';
import isWelcomeUser from './cli.js';

isWelcomeUser();

const gamebase = (assignment, gameSelected) => {
  for (let i = 3; i > 0; i -= 1) {
    
  }
};

export default gameBase;
